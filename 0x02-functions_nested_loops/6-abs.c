#include "main.h"

/**
 * _abs - write a function that computes the absolute value of an
 * integer
 * @int: int type
 *
 * Return: 0
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else 
	{
		return (n);
	}
}
